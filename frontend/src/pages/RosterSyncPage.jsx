import React from 'react';

// placeholder!! this isnt the real roster sync page, just here so
// DashboardPage.jsx doesnt break when it imports this. andre's actual
// page component should replace this file

export default function RosterSyncPage() {
  return (
    <div style={{ padding: 40, fontFamily: 'sans-serif', color: '#8A8D99' }}>
      Placeholder — swap in the real RosterSyncPage.jsx from andre.
    </div>
  );
}
